{
  "name": "branchlock-mcp",
  "version": "0.1.0",
  "private": true,
  "description": "Multi-Agent Workspace Lock & Semantic Collision Detector for AI coding agents",
  "author": "jamiejustcodes",
  "license": "MIT",
  "workspaces": [
    "shared",
    "daemon",
    "adapter",
    "client"
  ],
  "scripts": {
    "dev": "concurrently -n daemon,client -c blue,green \"npm run dev -w daemon\" \"npm run dev -w client\"",
    "build": "npm run build -w shared && npm run build -w daemon && npm run build -w adapter && npm run build -w client",
    "adapter": "node adapter/dist/index.js",
    "daemon": "node daemon/dist/index.js"
  },
  "devDependencies": {
    "concurrently": "^9.1.2",
    "typescript": "^5.7.3"
  }
}
